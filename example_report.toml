# report.toml
#
# title: レポートのタイトル（オプション、デフォルト: "Kiboku Report"）
# sections: セクションの表示順（縦に並ぶ順序）
# hidden: セクションを非表示（sections に含まれていても隠せる）
#
# 利用可能な section id:
# - package_summary         : パッケージ数 + ファイル数（C++/Python/launch/urdf/xacro/mesh）
# - workspace_dependencies  : パッケージ依存（グラフ/〇×マトリクス切替）
# - external_dependencies   : 外部ライブラリ依存（グラフ/〇×マトリクス切替）
# - findings                : findings表示（パッケージ別/ルール別切替）
# - findings_matrix         : 縦=パッケージ 横=findings(ルール) の件数表（クリックでソート）
# - external_libraries      : 外部ライブラリ一覧 + repo（external_repos から補完）
#
# section_heights: セクションごとに「グラフ/マトリクスのパネル高さ」を上書き（任意）
# - 値は CSS の長さ（例: "700px", "60vh"）
# - String / Integer / Float のみ対応（Float は px に丸める。それ以外: bool/array/table/datetime/... は無視）

title = "This is sample of Kiboku Report"

sections = [
  "package_summary",
  "findings",
  "findings_matrix",
  "external_libraries",
  "workspace_dependencies",
  "external_dependencies",

]

hidden = []

# セクションごとのパネル高さ（任意）
[section_heights]
workspace_dependencies = "900px"
external_dependencies = "700px"

# 外部ライブラリ名 -> リポジトリURL（任意）
[external_repos]
roscpp = "https://github.com/ros/ros_comm"

