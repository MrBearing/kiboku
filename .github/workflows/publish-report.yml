name: Generate and Publish Report

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout kiboku repository
        uses: actions/checkout@v4
        with:
          path: kiboku

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            kiboku/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('kiboku/Cargo.lock') }}

      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true

      - name: Build kiboku
        run: |
          cd kiboku
          cargo build --release
          echo "$(pwd)/target/release" >> $GITHUB_PATH

      - name: Checkout sample ROS workspace (moveit)
        uses: actions/checkout@v4
        with:
          repository: moveit/moveit
          ref: master
          path: moveit

      - name: Generate ROS1 analysis JSON
        run: |
          cd moveit
          bok analyze --platform ros1 -f json -o ../analysis-ros1.json .

      - name: Copy ROS1 analysis JSON into site output
        run: |
          mkdir -p report_output/ros1
          cp analysis-ros1.json report_output/ros1/analysis.json

      - name: Generate ROS1 HTML report
        run: |
          bok report analysis-ros1.json -o report_output/ros1/ --config kiboku/docs/github_pages_report.toml

      - name: Checkout sample ROS2 workspace (moveit2)
        uses: actions/checkout@v4
        with:
          repository: moveit/moveit2
          ref: main
          path: moveit2

      - name: Generate ROS2 analysis JSON
        run: |
          cd moveit2
          bok analyze --platform ros2 -f json -o ../analysis-ros2.json .

      - name: Copy ROS2 analysis JSON into site output
        run: |
          mkdir -p report_output/ros2
          cp analysis-ros2.json report_output/ros2/analysis.json

      - name: Generate ROS2 HTML report
        run: |
          bok report analysis-ros2.json -o report_output/ros2/ --config kiboku/docs/github_pages_report_ros2.toml

      - name: Copy portal index.html
        run: |
          cp kiboku/docs/index.html report_output/index.html

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: report_output

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
